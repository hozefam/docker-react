sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t hozefam/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true hozefam/docker-react npm run test
